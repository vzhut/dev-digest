import { HomeView } from "./_components/HomeView";

/* Route: / (Home). Thin route entry — redirect-or-onboarding logic lives in _components/HomeView. */
export default function HomePage() {
  return <HomeView />;
}
