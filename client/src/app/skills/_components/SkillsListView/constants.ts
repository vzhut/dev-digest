/** Width of the left (list) column; below this the two columns stack. */
export const LIST_COL_WIDTH = 380;
/** Minimum width of the right pane before it wraps under the list. */
export const DETAIL_MIN_WIDTH = 480;
