/** Grace period before closing, so the pointer can travel from trigger to popover. */
export const CLOSE_DELAY_MS = 150;

export const POPOVER_WIDTH = 400;
/** Distance between the trigger and the popover. */
export const POPOVER_GAP = 6;
