# docs — e2e

Deep-dives for the browser suite: how to write flows that stay deterministic, and how to debug them.
Not here: flow JSON format, env knobs, local/CI run instructions → `../README.md` · the flows themselves → `../specs/` · findings → `../INSIGHTS.md`.
Linked from `e2e/AGENTS.md` via *Use when*.

- [`writing-flows.md`](writing-flows.md) — new-flow checklist, assertion patterns (including asserting that something disappeared without CSS selectors), gotchas, debugging a failing step.
